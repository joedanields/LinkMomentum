"""API package placeholder.

Split application routes into modules under this package (e.g. upload.py,
auth.py, posts.py) and register routers from `main.py` using
`app.include_router(...)`.
"""
from fastapi import APIRouter

router = APIRouter()

# Example: from . import upload, auth, posts  # register sub-routers here
